<a href = "users">Users</a> |
<a href = "users/new">New User</a>

<hr/>

<% if current_user %>
  <p>Hello, <%= current_user.name %>. You are logged in as <%= current_user.username %>.</p>
  <%= link_to "Logout", logout_path %><br/>
<% else %>
  <%= link_to "Login", login_path %><br/>
<% end %>