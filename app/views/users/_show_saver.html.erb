<% unless friend_id==user_id %>
  <% if Friendship.find_by_user_id_and_friend_id(user_id, friend_id) || Friendship.find_by_user_id_and_friend_id(friend_id, user_id) %>
    (<%= link_to "Forget #{h(@user.first_name)}", friendship_path(:friend_id => friend_id), :method => :delete, :controller => :friendships %>)
  <% else %>
    (<%= link_to "Save #{h(@user.first_name)}", friendships_path(:friend_id => friend_id), :method => :post %>)
  <% end %>
<% end %>
